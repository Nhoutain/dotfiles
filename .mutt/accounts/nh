# vim: ft=muttrc
color status     brightwhite  black
color indicator  brightwhite  black

set from      = "houtainicolas@gmail.com"
set sendmail  = "~/.mutt/bin/msmtp-enqueue.sh -a nh"
set spoolfile = "+nh/nh.INBOX"
set mbox      = "+nh/nh.archive"
set record    = "+nh/nh.send"
set postponed = "+nh/nh.drafts" 
set query_command = "docker run --rm --volumes-from gc-config \
  pbrisbin/google-contacts mutt-query houtainicolas@gmail.com '%s'"

macro index ,c "<change-folder>+nh/nh.INBOX<enter>                   "  "change accounts"
macro index ,z "<shell-escape>mbsync --pull-new gmail-inbox<enter>   "  "sync IMAP"
macro index D  "<save-message>+nh/nh.trash<enter>                    "  "move message to the trash"
macro index S  "<save-message>+nh/nh.spam<enter>                     "  "mark message as spam"

#macro index ,D \
#  "<tag-pattern>all<enter><tag-prefix><save-message>+NH/trash<enter>" \
#  "delete all"


